import { describe, expect, it } from 'vitest'
import { transformPdf } from '../src/pdf'

describe('test', () => {
  it('sum should return 2', () => {
    expect(transformPdf('')).toMatchInlineSnapshot()
  })
})
